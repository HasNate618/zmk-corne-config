# Helps BLE pairing on some Linux adapters (ZMK troubleshooting docs)
CONFIG_BT_CTLR_PHY_2M=n
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
# Clear stale host bonds on boot (left/central half only)
CONFIG_ZMK_BLE_CLEAR_BONDS_ON_START=y
